# Database (SSL disabled for development)
DATABASE_URL="postgresql://zipperoo:zipperoo123@postgres:5432/zipperoo_db?schema=public&sslmode=disable"

# JWT - 生產環境建議使用更安全的密鑰
JWT_SECRET="jwt-secret-key"
JWT_EXPIRES_IN="1h"
REFRESH_JWT_SECRET="jwt-secret-key-refresh"
REFRESH_JWT_EXPIRES_IN="3h"

# Server
BACKEND_PORT=3000

# CORS - 生產環境配置
CORS_ORIGIN="*"

# Frontend - 讓前端自動使用當前域名
VITE_API_BASE="http://localhost"
