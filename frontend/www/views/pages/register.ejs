<!DOCTYPE html>
<html lang="zh-TW">
<head>
  <%- include('../partials/head', { 
    title: '註冊 - Zipperoo', 
    description: '建立您的 Zipperoo 帳戶'
  }) %>
</head>
<body>
  <!-- Navigation -->
  <%- include('../partials/navbar', { user: locals.user }) %>

  <!-- Register Form -->
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-md-8 col-lg-6">
        <div class="card border-0 shadow-sm">
          <div class="card-body p-4">
            <div class="text-center mb-4">
              <h1 class="h4 fw-bold">建立帳戶</h1>
              <p class="text-muted">立即加入 Zipperoo</p>
            </div>

            <% if (locals.error) { %>
              <div class="alert alert-danger" role="alert">
                <i class="bi bi-exclamation-triangle me-2"></i>
                <%= error %>
              </div>
            <% } %>

            <!-- Account Type Selection -->
            <div class="mb-4">
              <label class="form-label fw-bold">帳戶類型</label>
              <div class="row g-3">
                <div class="col-md-6">
                  <div class="card account-type-card" data-type="BUYER">
                    <div class="card-body text-center">
                      <i class="bi bi-person fs-1 text-primary mb-2"></i>
                      <h6>買家</h6>
                      <small class="text-muted">購買商品</small>
                    </div>
                  </div>
                </div>
                <div class="col-md-6">
                  <div class="card account-type-card" data-type="SELLER">
                    <div class="card-body text-center">
                      <i class="bi bi-shop fs-1 text-success mb-2"></i>
                      <h6>賣家</h6>
                      <small class="text-muted">販售商品</small>
                    </div>
                  </div>
                </div>
              </div>
            </div>

            <form id="register-form">
              <input type="hidden" id="role" name="role" value="BUYER">

              <div class="mb-3">
                <label for="account" class="form-label">帳號 <span class="text-danger">*</span></label>
                <input type="text" class="form-control" id="account" name="account" required minlength="3" placeholder="請輸入您的帳號">
                <div class="form-text">帳號最少需要 3 個字元，建議使用英文或數字</div>
              </div>

              <div class="mb-3">
                <label for="username" class="form-label">用戶名稱 <span class="text-danger">*</span></label>
                <input type="text" class="form-control" id="username" name="username" required minlength="2" placeholder="請輸入您的姓名">
                <div class="form-text">您的顯示名稱，最少需要 2 個字元</div>
              </div>

              <div class="mb-3">
                <label for="email" class="form-label">電子郵件</label>
                <input type="email" class="form-control" id="email" name="email" placeholder="請輸入您的電子郵件（可選）">
                <div class="form-text">用於接收重要通知，非必填</div>
              </div>

              <div class="mb-3">
                <label for="phone" class="form-label">手機號碼</label>
                <input type="tel" class="form-control" id="phone" name="phone" placeholder="例如：+886912345678">
                <div class="form-text">用於訂單聯繫，非必填</div>
              </div>

              <div class="mb-3">
                <label for="password" class="form-label">密碼 <span class="text-danger">*</span></label>
                <div class="input-group">
                  <input type="password" class="form-control" id="password" name="password" required minlength="6" placeholder="請輸入您的密碼" autocomplete="new-password">
                  <button class="btn btn-outline-secondary" type="button" id="togglePassword">
                    <i class="bi bi-eye" id="togglePasswordIcon"></i>
                  </button>
                </div>
                <div class="form-text">密碼至少需要 6 個字元</div>
              </div>

              <div class="mb-3">
                <label for="confirmPassword" class="form-label">確認密碼 <span class="text-danger">*</span></label>
                <div class="input-group">
                  <input type="password" class="form-control" id="confirmPassword" name="confirmPassword" required placeholder="請再次輸入密碼" autocomplete="new-password">
                  <button class="btn btn-outline-secondary" type="button" id="toggleConfirmPassword">
                    <i class="bi bi-eye" id="toggleConfirmPasswordIcon"></i>
                  </button>
                </div>
              </div>

              <!-- Seller-specific fields -->
              <div id="seller-fields" class="d-none">
                <div class="mb-3">
                  <label for="shopName" class="form-label">商店名稱</label>
                  <input type="text" class="form-control" id="shopName" name="shopName" placeholder="請輸入您的商店名稱">
                  <div class="form-text">賣家專用：您的商店顯示名稱</div>
                </div>
                <div class="mb-3">
                  <label for="description" class="form-label">商店描述</label>
                  <textarea class="form-control" id="description" name="description" rows="3" placeholder="簡單描述您的商店特色"></textarea>
                  <div class="form-text">賣家專用：簡單介紹您的商店</div>
                </div>
              </div>

              <div class="mb-3 form-check">
                <input type="checkbox" class="form-check-input" id="agree" required>
                <label class="form-check-label" for="agree">
                  我同意 <a href="/terms" target="_blank">服務條款</a> 
                  和 <a href="/privacy" target="_blank">隱私政策</a>
                </label>
              </div>

              <button type="submit" class="btn btn-primary w-100 mb-3" id="register-btn">
                <span class="btn-text">建立帳戶</span>
                <div class="loading d-none" style="width: 20px; height: 20px; margin: 0 auto;"></div>
              </button>
            </form>

            <div class="text-center">
              <p class="text-muted">
                已經有帳戶了？ 
                <a href="/login" class="text-decoration-none fw-bold">立即登入</a>
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <%- include('../partials/footer') %>

  <!-- Scripts -->
  <%- include('../partials/scripts') %>
</body>
</html> 